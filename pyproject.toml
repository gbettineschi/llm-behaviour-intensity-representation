[tool.uv]
package = false

[project]
name = "llm-behaviour-intensity-representation"
version = "0.1.0"
description = "Testing linearity of trait intensity representations in Gemma 2 2B activation space"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "transformers",
    "torch",
    "numpy",
    "scipy",
    "matplotlib",
    "pyyaml",
    "jupyter>=1.1.1",
    "nb-clean>=4.0.1",
    "pandas>=2.2.0",
    "scikit-learn>=1.5.0",
    "litellm>=1.60.0",
    "jsonschema>=4.20.0",
    "python-dotenv>=1.0.0",
]
